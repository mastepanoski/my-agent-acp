# ACP Agent Configuration Example
# Copy this file to .env and configure your settings

# Server Configuration
ACP_HOST=localhost
ACP_PORT=3000
ENABLE_CORS=true

# LM Studio Configuration
LM_STUDIO_URL=http://localhost:1234/v1
LM_STUDIO_MODEL=llama-3.2-3b-instruct
LM_STUDIO_TIMEOUT=30000

# Agent Configuration
AGENT_NAME=AsistentePersonal
AGENT_DESCRIPTION=Un agente de IA que puede ayudar con tareas generales
AGENT_VERSION=1.0.0

# Logging Configuration
NODE_ENV=development
LOG_LEVEL=debug

# Runtime Selection (node or bun)
RUNTIME=node

# Optional: Monitoring and Observability
# ENABLE_METRICS=true
# METRICS_PORT=9100

# Optional: Database (if using persistent storage)
# DATABASE_URL=sqlite:./data/agent.db

# Optional: Authentication (if implementing auth)
# JWT_SECRET=your-jwt-secret-here
# API_KEY=your-api-key-here

# Optional: Rate Limiting
# RATE_LIMIT_WINDOW=15
# RATE_LIMIT_MAX=100

# Optional: HTTPS Configuration
# HTTPS_ENABLED=false
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem