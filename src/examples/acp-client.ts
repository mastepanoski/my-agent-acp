// TODO: Update examples to use ACP SDK client
// This should be replaced with the official ACP client from the SDK

/*
Example usage with ACP SDK:

import { Client } from 'acp-sdk';

const client = new Client({ baseUrl: "http://localhost:8000" });
const run = await client.runSync("my-agent", "Hello!");
run.output.forEach((message) => console.log(message));
*/

export {}; // Make this a valid module
